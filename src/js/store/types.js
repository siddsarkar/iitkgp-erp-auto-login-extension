/**
 * ------------------ ADDING TYPES -------------
 * 1. Create and test api to receive correct data
 * 2. Define type here
 * 3. Modify ui elements
 * 4. Create the action and response(simply log for response)
 * 5. Connect the api at background-js
 */

export const SYNC_USER = 'SYNC_USER'
export const GET_REPOS = 'GET_REPOS'
export const GET_STARRED_REPOS = 'GET_STARRED_REPOS'
